{
  "config": {
    "step": {
      "user": {
        "title": "Important Entities Watchdog",
        "description": "Pick a label and the period after which entities are considered silent.",
        "data": {
          "label": "Label",
          "period": "Silent threshold",
          "realtime": "Real-time mode"
        },
        "data_description": {
          "realtime": "Re-evaluate entity freshness on every source state event. Off by default; with this off, the watchdog only checks periodically (every 1/10 of the silent threshold), which is much cheaper for high-frequency sources."
        }
      }
    },
    "abort": {
      "already_configured": "This label and period combination is already configured.",
      "no_labels": "No labels found. Create a label in Home Assistant first and assign it to the entities you want to monitor."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Change watchdog settings",
        "data": {
          "period": "Silent threshold",
          "realtime": "Real-time mode"
        },
        "data_description": {
          "realtime": "Re-evaluate entity freshness on every source state event. Off by default; with this off, the watchdog only checks periodically (every 1/10 of the silent threshold), which is much cheaper for high-frequency sources."
        }
      }
    }
  }
}
